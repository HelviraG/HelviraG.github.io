{
  "name": "helvira_app_server",
  "version": "1.0.0",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "firebase": "^12.3.0",
    "netlify-cli": "^23.9.1",
    "serverless-http": "^4.0.0"
  },
  "scripts": {
    "build": "yarn build:client && yarn build:server",
    "build:client": "cd client && yarn build",
    "build:server": "tsc",
    "start": "netlify dev",
    "start:server": "cd server && npm start",
    "dev": "npm run dev:client & npm run start:server",
    "dev:client": "cd client && npm run start",
    "dev:server": "cd server && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/HelviraG/HelviraG.github.io"
  },
  "private": true,
  "devDependencies": {}
}
